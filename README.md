# normalized-convolution-reconstruction
This project implements the theory from Knutsson et al. on Normalized Convolution, which provides a principled method for computing local signal approximations even when the data contains unknown or unreliable samples.
